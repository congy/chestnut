#include "kandan_lg.h"
#include "kandan_lg_query.h"
#include "util.h"
#include "proto_kandan_lg.pb.h"
#include <zmq.h>
int main() {
  read_data();
    Query0Result q_0_result;
     query_0_plan_7(200,809339, q_0_result);
    Query1Result q_1_result;
     query_1_plan_3(107, q_1_result);
    Query2Result q_2_result;
     query_2_plan_3(185, q_2_result);
    Query3Result q_3_result;
     query_3_plan_2(2449150, q_3_result);
    Query4Result q_4_result;
     query_4_plan_13(243, q_4_result);
    Query5Result q_5_result;
     query_5_plan_14(416, q_5_result);
    Query6Result q_6_result;
     query_6_plan_1("Rachel Sullivan", q_6_result);
    Query7Result q_7_result;
     query_7_plan_0("Military key receive fish morning. Wide relate ability big forward son. Join sure window. Recognize after she. Hour behind method store personal hotel apply. Address gun example society own baby fire. Simple similar citizen. Behind dream watch economic street. Management write everybody remain during analysis. Concern human candidate ball. Get network situation behind news scientist little while. Physical prove lot issue drive small memory cut. Federal choice story material hour recently. Only beyond anything involve rate idea television about. Nearly recognize hour reveal street who. Apply red happen measure response. Catch each skill. Probably recent product federal deal example. Yeah after sea it these commercial. Tonight front over north several face. Future here discuss else. Traditional sense hope if. Watch despite goal these. Remember away game specific give. Service speak mother party director situation west. Stage admit national they. Always region them above.", q_7_result);

 return 0;
}
